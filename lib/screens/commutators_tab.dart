import 'package:flutter/material.dart';

class CommutatorsTab extends StatelessWidget {
  const CommutatorsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Commutators Content',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
