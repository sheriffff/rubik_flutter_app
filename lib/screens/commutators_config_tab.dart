import 'package:flutter/material.dart';

class CommutatorsConfigTab extends StatelessWidget {
  const CommutatorsConfigTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Commutators Config Content',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
