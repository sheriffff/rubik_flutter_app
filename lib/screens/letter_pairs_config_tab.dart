import 'package:flutter/material.dart';

class LetterPairsConfigTab extends StatelessWidget {
  const LetterPairsConfigTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Letter Pairs Config Content',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
