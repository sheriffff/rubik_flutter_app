import 'package:flutter/material.dart';

class StickerLettersTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Sticker Letters Content',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
